# Use Svelte Flow for the DAG canvas

Artio uses `@xyflow/svelte` (Svelte Flow) for graph canvas interaction, custom nodes, and connection mechanics. Artio retains responsibility for Workflow semantics, edge validity, revision handling, source edits, and Preview state rather than embedding those rules in the canvas library.
