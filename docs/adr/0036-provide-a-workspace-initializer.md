# Provide a workspace initializer

`artio init <workspace>` creates an `artio.toml` manifest and a valid managed Python module skeleton with the required Polars import and Artio declarations. This provides a parseable starting point for visual authoring and agent collaboration rather than requiring inference from arbitrary empty code.
