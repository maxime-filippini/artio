# Keep generic transformations code-authored

The DAG editor creates and connects named managed Transformations, but their generic Polars bodies are authored in code. Decision trees are the initial specialized visual expression editor; Artio does not begin with a parallel visual DSL for filters, joins, aggregations, or other Polars operations.
