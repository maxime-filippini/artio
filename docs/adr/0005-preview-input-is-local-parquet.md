# Preview input is local Parquet

The first Artio release accepts local Parquet files as Sample data for Preview execution. This deliberately narrows input handling to a columnar, schema-preserving format and defers remote connectors, credentials, and alternative file formats.
