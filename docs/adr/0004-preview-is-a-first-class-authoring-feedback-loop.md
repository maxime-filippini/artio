# Preview is a first-class authoring feedback loop

Artio automatically refreshes a bounded Preview after each valid workflow change so authors can inspect the effect of transformations and decision trees on Sample data. It reports parse or execution diagnostics when refresh is impossible, while production scheduling and orchestration remain outside the initial product boundary.
