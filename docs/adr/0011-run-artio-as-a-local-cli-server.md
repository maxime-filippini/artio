# Run Artio as a local CLI server

Artio starts its FastAPI server locally through a CLI and initially targets a single authoring workspace. Collaboration occurs through the local client and agent tools with revision checks; shared, multi-user editing and its merge semantics are deferred.
