# Provide a structured local MCP server

Artio provides a local MCP server in its first release so agents can inspect and modify workflows through structured tools for graph state, Decision trees, previews, and diagnostics. Direct filesystem editing remains supported through bidirectional reconciliation, but the MCP interface is the safer default for agent collaboration.
