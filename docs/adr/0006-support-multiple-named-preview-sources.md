# Support multiple named preview sources

Workflows can declare multiple named Sources, and each Source independently binds to a local Parquet Sample data file for Preview execution. This makes joins and other multi-input transformations first-class instead of treating them as a later exception to a single-input model.
